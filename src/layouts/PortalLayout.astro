---
import "../styles/global.css";

import {
  RiHome5Line,
  RiAddCircleLine,
  RiDashboardLine,
  RiUserLine,
  RiShoppingBag3Line,
  RiCustomerService2Line,
  RiLogoutBoxRLine,
  RiMenuFoldLine,
  RiMenuUnfoldLine,
  RiMenu3Line,
  RiCloseLine
} from "react-icons/ri";

const { title = "Portal AVYNA" } = Astro.props;
---

<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
</head>

<body class="min-h-screen bg-black text-white antialiased">

<!-- BG -->
<div class="fixed inset-0 -z-10 overflow-hidden">
  <div class="absolute inset-0 bg-black"></div>
  <div class="absolute left-[-10%] top-[-10%] h-[36rem] w-[36rem] rounded-full bg-white/[0.03] blur-3xl"></div>
  <div class="absolute bottom-[-10%] right-[-10%] h-[40rem] w-[40rem] rounded-full bg-white/[0.02] blur-3xl"></div>
</div>

<div class="flex min-h-screen">

  <!-- DESKTOP SIDEBAR -->
  <aside
    id="sidebar"
    class="hidden lg:flex h-screen sticky top-0 shrink-0 flex-col border-r border-white/10 bg-white/[0.025] backdrop-blur-2xl transition-all duration-300 w-[300px] min-w-[300px] overflow-hidden"
  >

    <!-- TOP -->
    <div class="border-b border-white/10 px-5 pt-5 pb-4">

      <div class="flex items-center justify-between gap-3">

        <div class="label-wrap min-w-0">
          <p class="text-[10px] uppercase tracking-[0.42em] text-white/28">
            AVYNA SALTILLO
          </p>

          <h1 class="mt-3 text-[30px] leading-[1.05] font-semibold tracking-tight">
            Portal
            <br />
            Privado
          </h1>
        </div>

        <button
          id="toggleSidebar"
          class="grid h-11 w-11 place-items-center rounded-2xl border border-white/10 bg-white/[0.03] text-white/70 transition hover:bg-white/10 hover:text-white"
        >
          <RiMenuFoldLine id="iconOpen" size={19}/>
          <RiMenuUnfoldLine id="iconClose" size={19} className="hidden"/>
        </button>

      </div>

    </div>

    <!-- NAV -->
    <nav class="flex-1 overflow-y-auto px-4 py-5 space-y-2">

      <a href="/portal/" class="nav-link">
        <RiHome5Line size={20}/>
        <span class="label-wrap">Inicio</span>
      </a>

      <a href="/portal/new-order" class="nav-link">
        <RiAddCircleLine size={20}/>
        <span class="label-wrap">Pedido nuevo</span>
      </a>

      <a href="/portal/dashboard" class="nav-link">
        <RiDashboardLine size={20}/>
        <span class="label-wrap">Dashboard</span>
      </a>

      <div class="my-4 border-t border-white/10"></div>

      <a href="/portal/profile" class="nav-link">
        <RiUserLine size={20}/>
        <span class="label-wrap">Mi perfil</span>
      </a>

      <a href="/portal/orders" class="nav-link">
        <RiShoppingBag3Line size={20}/>
        <span class="label-wrap">Pedidos</span>
      </a>

      <a href="/portal/support" class="nav-link">
        <RiCustomerService2Line size={20}/>
        <span class="label-wrap">Soporte</span>
      </a>

    </nav>

    <!-- FOOT -->
    <div class="mt-auto border-t border-white/10 p-4">

      <div class="rounded-[28px] border border-white/10 bg-black/45 p-4">

        <div class="flex items-start gap-3">

          <div
            id="sidebarAvatar"
            class="grid h-11 w-11 place-items-center rounded-2xl bg-white text-black font-semibold"
          >
            A
          </div>

          <div class="min-w-0 label-wrap">
            <p id="sidebarName" class="truncate font-semibold">
              Cliente
            </p>

            <p id="sidebarEmail" class="mt-1 truncate text-xs text-white/42">
              cargando...
            </p>
          </div>

        </div>

        <button
          id="logoutBtn"
          class="mt-4 flex h-12 w-full items-center justify-center gap-2 rounded-2xl bg-white font-semibold text-black"
        >
          <RiLogoutBoxRLine size={18}/>
          <span class="label-wrap">Cerrar sesión</span>
        </button>

      </div>

    </div>

  </aside>

  <!-- MOBILE MENU -->
  <div
    id="mobileOverlay"
    class="fixed inset-0 z-40 hidden bg-black/70 backdrop-blur-sm lg:hidden"
  ></div>

  <aside
    id="mobileSidebar"
    class="fixed left-0 top-0 z-50 h-screen w-[290px] -translate-x-full border-r border-white/10 bg-black/95 backdrop-blur-2xl transition-transform duration-300 lg:hidden"
  >

    <div class="border-b border-white/10 px-5 py-5 flex items-center justify-between">

      <div>
        <p class="text-[10px] uppercase tracking-[0.35em] text-white/30">
          AVYNA
        </p>

        <p class="mt-1 font-semibold">
          Portal Privado
        </p>
      </div>

      <button
        id="closeMobileMenu"
        class="grid h-10 w-10 place-items-center rounded-xl border border-white/10"
      >
        <RiCloseLine size={20}/>
      </button>

    </div>

    <nav class="p-4 space-y-2">

      <a href="/portal/" class="nav-link mobile-link">
        <RiHome5Line size={20}/>
        <span>Inicio</span>
      </a>

      <a href="/portal/new-order" class="nav-link mobile-link">
        <RiAddCircleLine size={20}/>
        <span>Pedido nuevo</span>
      </a>

      <a href="/portal/dashboard" class="nav-link mobile-link">
        <RiDashboardLine size={20}/>
        <span>Dashboard</span>
      </a>

      <div class="my-4 border-t border-white/10"></div>

      <a href="/portal/profile" class="nav-link mobile-link">
        <RiUserLine size={20}/>
        <span>Mi perfil</span>
      </a>

      <a href="/portal/orders" class="nav-link mobile-link">
        <RiShoppingBag3Line size={20}/>
        <span>Pedidos</span>
      </a>

      <a href="/portal/support" class="nav-link mobile-link">
        <RiCustomerService2Line size={20}/>
        <span>Soporte</span>
      </a>

    </nav>

  </aside>

  <!-- MAIN -->
  <div class="flex min-h-screen flex-1 flex-col">

    <!-- MOBILE HEADER -->
    <header class="lg:hidden sticky top-0 z-30 border-b border-white/10 bg-black/80 backdrop-blur-xl">

      <div class="flex items-center justify-between px-4 py-4">

        <div class="flex items-center gap-3">

          <button
            id="openMobileMenu"
            class="grid h-10 w-10 place-items-center rounded-xl border border-white/10"
          >
            <RiMenu3Line size={20}/>
          </button>

          <div>
            <p class="text-[10px] uppercase tracking-[0.35em] text-white/35">
              AVYNA
            </p>

            <p class="text-sm font-semibold">
              Portal Privado
            </p>
          </div>

        </div>

        <button
          id="mobileLogout"
          class="rounded-xl bg-white px-4 py-2 text-sm font-semibold text-black"
        >
          Salir
        </button>

      </div>

    </header>

    <main class="flex-1 p-4 sm:p-6 lg:p-8">
      <div class="mx-auto max-w-7xl">
        <slot />
      </div>
    </main>

  </div>

</div>

<style>
.nav-link{
display:flex;
align-items:center;
gap:14px;
height:56px;
padding:0 16px;
border-radius:18px;
color:rgba(255,255,255,.72);
transition:.22s ease;
white-space:nowrap;
}

.nav-link:hover:not(.active-link){
background:rgba(255,255,255,.06);
color:#fff;
transform:translateX(3px);
}

.active-link{
background:#fff;
color:#000 !important;
font-weight:700;
pointer-events:none;
}

.active-link svg{
color:#000 !important;
}

/* collapsed desktop */
.sidebar-collapsed{
width:96px !important;
min-width:96px !important;
}

.sidebar-collapsed .label-wrap{
display:none !important;
}

.sidebar-collapsed .nav-link{
width:58px;
height:58px;
padding:0;
justify-content:center;
}

.sidebar-collapsed nav{
padding:18px 0;
display:flex;
flex-direction:column;
align-items:center;
}

.sidebar-collapsed .nav-link span{
display:none;
}

.sidebar-collapsed #logoutBtn span{
display:none;
}

/* mobile products responsive */
@media (max-width:1023px){

#productsGrid .grid{
grid-template-columns:1fr 90px 120px !important;
}

#productsGrid .hide-mobile{
display:none !important;
}

#productsGrid .product-name{
font-size:14px;
line-height:1.3;
white-space:normal !important;
}

}
</style>

<script>
  import "../scripts/portal.ts";
</script>

</body>
</html>